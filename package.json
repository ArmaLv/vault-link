{
	"name": "vault-link",
	"version": "0.1.5",
	"description": "Link files and folders between Obsidian vaults via symlinks, with per-file exclude rules.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [],
	"author": "ArmaLv",
	"license": "MIT",
	"dependencies": {
		"@types/node": "^20.11.0"
	},
	"devDependencies": {
		"esbuild": "0.19.11",
		"obsidian": "latest",
		"tslib": "2.6.2",
		"typescript": "5.3.3"
	}
}
